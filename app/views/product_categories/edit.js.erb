// Show the edit form in the right box.
heading = "Edit Product Categories";
bodyContent = "<%= escape_javascript(render(:partial=>"form")) %>";
showModal(heading, bodyContent);
validatedoctorForm();
